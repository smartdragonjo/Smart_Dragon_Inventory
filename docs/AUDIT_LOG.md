# سجل التعديلات

صفحة Owner-only لعرض آخر 300 حدث إداري من `audit_logs`.

يتم تسجيل عمليات النشر والحذف والاعتماد والرفض وإدارة الأصناف والمستخدمين واستيراد البيانات. ابتداءً من v8 يتم أيضًا تسجيل إنشاء طلبات المراجعة من حسابات Editor.

سجلات التدقيق غير قابلة للتعديل أو الحذف حسب Firestore Rules.
